# ===========================
# Configuración del Servidor
# ===========================
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# URL del frontend para CORS - Usado en index.js
FRONTEND_URL=http://localhost:4200

# ===========================
# Base de Datos MongoDB
# ===========================
MONGODB_URI=mongodb://localhost:27017/life

# ===========================
# JWT
# ===========================
JWT_SECRET=mi_super_secreto_para_jwt
JWT_EXPIRES_IN=1d

# ===========================
# OpenAI
# ===========================
OPENAI_API_KEY=tu_api_key_openai
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_TEMPERATURE=0.7
OPENAI_API_MAX_TOKENS=1000
OPENAI_API_TOP_P=1
OPENAI_API_FREQUENCY_PENALTY=0
OPENAI_API_PRESENCE_PENALTY=0
OPENAI_API_STOP_SEQUENCE=["\n\n", "\n"]
OPENAI_API_SYSTEM_MESSAGE="You are a helpful assistant."
OPENAI_API_USER_MESSAGE="You are a helpful assistant."
OPENAI_API_MAX_RETRIES=3

# ===========================
# WhatsApp Business
# ===========================
WHATSAPP_PHONE_NUMBER_ID=123456789012345
WHATSAPP_BUSINESS_ACCOUNT_ID=9876543210
WHATSAPP_ACCESS_TOKEN=tu_token_permanente_de_whatsapp
WHATSAPP_VERIFY_TOKEN=mi_token_verificacion_webhook

# ===========================
# Webhooks
# ===========================
WEBHOOK_URL=https://tuservidor.com/webhook/whatsapp

# =============================================================================
# LOGGING (OPCIONAL)
# =============================================================================
# Log Level - Usado en src/config/logger.js
# Valores: error, warn, info, http, debug
LOG_LEVEL=info
# Directorio de logs - Usado en src/config/logger.js (solo en producción)
LOG_DIR=./logs

# =============================================================================
# RATE LIMITING (OPCIONAL)
# =============================================================================
# Límite de requests por IP - Usado en index.js
# Fallback: 100 si no se define
RATE_LIMIT_MAX=100


# =============================================================================
# COOKIES Y SESIONES (OPCIONAL - SOLO PARA PRODUCCIÓN)
# =============================================================================
# Dominio para cookies - Usado en authentication.index.js
# Solo necesario en producción con dominio personalizado
# COOKIE_DOMAIN=tudominio.com

# =============================================================================
# SERVICIOS EXTERNOS (FUTURO - NO IMPLEMENTADO AÚN)
# =============================================================================
# Estas variables están preparadas para futuras funcionalidades:

# OpenAI para traducciones (cuando se implemente)
# OPENAI_API_KEY=tu-openai-api-key-aqui

# Google Maps para geolocalización (cuando se implemente)
# GOOGLE_MAPS_API_KEY=tu-google-maps-api-key

# Redis para cache (cuando se implemente)
# REDIS_URL=redis://localhost:6379

# =============================================================================
# NOTA IMPORTANTE
# =============================================================================
# Variables que NO están siendo usadas actualmente y fueron removidas:
# - MONGODB_MAX_CONNECTIONS, MONGODB_TIMEOUT (no implementado en database.mongo.js)
# - COOKIE_SECURE (se determina automáticamente por NODE_ENV)
# - RATE_LIMIT_WINDOW (no implementado en index.js)
# - SESSION_SECRET (no se usa, solo JWT_SECRET)
# - MAX_CONCURRENT_SESSIONS (hardcodeado en AuthConfig)
# - Todas las configuraciones OAuth (no implementadas aún)
# - Configuraciones SMTP/SendGrid (no implementadas aún)
# - Configuraciones AWS/Cloudinary (no implementadas aún)